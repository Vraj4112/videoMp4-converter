<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if (locals.title) { %>
        <title><%= title %></title>
    <% } %>
    
</head>
<body>
    <% if (locals.message) { %>
        <h1><%= message %></h1>
    <% } %>
    <a href="/">Go back to home</a>
</body>
</html>